from collections import namedtuple

from face_detection.face_detectors import HaarFaceDetector, DlibFaceDetector, \
    Box, GREEN, RED, front_cascPath, profile_cascPath

# Box = namedtuple('Box', ['top', 'right', 'bottom', 'left'])
# GREEN = (0, 255, 0)
# RED = (0, 0, 255)
